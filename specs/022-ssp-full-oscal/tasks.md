# Tasks: Feature 022 — SSP 800-18 Full Sections + OSCAL Output

**Input**: Design documents from `/specs/022-ssp-full-oscal/`
**Prerequisites**: plan.md (required), spec.md (required), research.md, data-model.md, contracts/mcp-tools.md, quickstart.md

## Format: `[ID] [P?] [Story?] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3, US4, US5)
- Exact file paths included in descriptions

## User Stories (from spec.md Part 5)

| Story | Title | Priority | Caps |
|-------|-------|----------|------|
| US1 | SSP Section Management | P1 (MVP) | Cap 1.1–1.4 |
| US2 | Complete SSP Section Generation | P2 | Cap 2.1–2.10 |
| US3 | OSCAL 1.1.2 SSP Export | P3 | Cap 3.1–3.8 |
| US4 | OSCAL Structural Validation | P4 | Cap 4.1–4.3 |
| US5 | Enhanced SSP Generation | P5 | Cap 5.1–5.5 |

---

## Phase 1: Setup

**Purpose**: Verify baseline and confirm pre-implementation readiness

- [x] T001 Verify `dotnet build Ato.Copilot.sln` and `dotnet test` pass as pre-implementation baseline

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Entity layer, DbContext, and interface signatures shared by all user stories

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [x] T002 Add `SspSectionStatus` enum (NotStarted, Draft, UnderReview, Approved) and `OperationalStatus` enum (Operational, UnderDevelopment, Disposed, MajorModification) with XML documentation in `src/Ato.Copilot.Core/Models/Compliance/RmfModels.cs`
- [x] T003 Add `SspSection` entity class with 15 members (14 data fields + 1 navigation property) (Id [Key, MaxLength(36)], RegisteredSystemId [Required, MaxLength(36)], SectionNumber [Required, Range(1,13)], SectionTitle [Required, MaxLength(200)], Content [MaxLength(32000)], Status [Required, SspSectionStatus], IsAutoGenerated, HasManualOverride, AuthoredBy [Required, MaxLength(200)], AuthoredAt, ReviewedBy [MaxLength(200)], ReviewedAt, ReviewerComments [MaxLength(4000)], Version [ConcurrencyCheck] default 1, RegisteredSystem nav prop) per data-model.md in `src/Ato.Copilot.Core/Models/Compliance/RmfModels.cs`
- [x] T004 Add `ContingencyPlanReference` entity class (15 fields: Id, RegisteredSystemId, DocumentTitle, DocumentLocation, DocumentVersion, LastTestedDate, TestType, RecoveryTimeObjective, RecoveryPointObjective, AlternateProcessingSite, BackupProceduresSummary, CreatedBy, CreatedAt, ModifiedAt, RegisteredSystem nav prop) per data-model.md and extend `RegisteredSystem` with DitprId [MaxLength(50)], EmassId [MaxLength(50)], OperationalStatus [OperationalStatus?], OperationalDate [DateTime?], DisposalDate [DateTime?], SspSections [ICollection nav prop], ContingencyPlanReference [nav prop] in `src/Ato.Copilot.Core/Models/Compliance/RmfModels.cs`
- [x] T005 Add `SspSections` and `ContingencyPlanReferences` DbSets and OnModelCreating configuration: SspSection (key, property constraints, Status enum-to-string conversion [MaxLength(20)], Version as IsConcurrencyToken, unique index IX_SspSection_System_Number on RegisteredSystemId+SectionNumber, status index IX_SspSection_Status); RegisteredSystem HasMany SspSections cascade delete + HasOne ContingencyPlanReference cascade delete + OperationalStatus enum-to-string conversion; ContingencyPlanReference (key, property constraints) per data-model.md in `src/Ato.Copilot.Core/Data/Context/AtoCopilotContext.cs`
- [x] T006 Add `WriteSspSectionAsync` (with `submitForReview` bool parameter), `ReviewSspSectionAsync`, and `GetSspCompletenessAsync` method signatures (per contracts/mcp-tools.md ISspService section) to the `ISspService` interface in `src/Ato.Copilot.Agents/Compliance/Services/ISspService.cs`

**Checkpoint**: Foundation ready — entity layer, DbContext, and interface signatures complete. User story implementation can begin.

---

## Phase 3: User Story 1 — SSP Section Management (Priority: P1) 🎯 MVP

**Goal**: Enable structured authoring, review, and tracking of individual SSP sections with lifecycle management (NotStarted→Draft→UnderReview→Approved).

**Independent Test**: Author §6 section → verify SspSection created with Status=Draft, Version=1. Update it → verify Version=2, Status reset to Draft. Submit for review → set to UnderReview. Approve → Status=Approved. Run completeness → verify readiness percentage and blocking issues.

### Implementation

- [x] T007 [US1] Implement `WriteSspSectionAsync` in SspService: section classification logic (auto-generated §1,§2,§3,§4,§7,§9,§10,§11 regenerate from data; authored §5,§8,§12,§13 store markdown content; hybrid §6 merge auto-populated cloud region with authored narrative); first write creates SspSection with Version=1 Status=Draft; subsequent writes increment Version and reset Status to Draft even if Approved; support `submit_for_review` flag that transitions Draft→UnderReview after writing (reject with INVALID_STATUS_FOR_SUBMIT if not in Draft); optimistic concurrency check (reject if expected_version != stored Version); HasManualOverride=true when user provides content for auto-generated section; validate section_number in 1–13 range in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T008 [US1] Implement `ReviewSspSectionAsync` in SspService: validate section exists (SECTION_NOT_FOUND); validate Status==UnderReview (INVALID_STATUS_FOR_REVIEW); approve → Status=Approved + ReviewedBy + ReviewedAt; request_revision → Status=Draft + ReviewedBy + ReviewedAt + ReviewerComments (COMMENTS_REQUIRED if missing); reject invalid decision values (INVALID_DECISION) in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T009 [US1] Implement `GetSspCompletenessAsync` in SspService: query SspSections for system (left-join all 13 section numbers); return per-section summary with section_number, section_title (NIST 800-18 title), status, is_auto_generated, has_manual_override, authored_by, authored_at, word_count (Content?.Split().Length), version; compute overall_readiness_percent = (approved_count / 13.0 * 100); generate blocking_issues list for all non-Approved sections with reason in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T010 [P] [US1] Create `WriteSspSectionTool` class extending BaseTool (Name="compliance_write_ssp_section", Description per contracts, RequiredPimTier=PimTier.Write, Parameters: system_id [string, required], section_number [int, required], content [string, conditional], authored_by [string, required], expected_version [int, optional], submit_for_review [bool, default false]; ExecuteCoreAsync resolves system via TryResolveSystemIdAsync then calls WriteSspSectionAsync; error codes: SYSTEM_NOT_FOUND, INVALID_SECTION_NUMBER, CONTENT_REQUIRED, CONCURRENCY_CONFLICT, INVALID_STATUS_FOR_SUBMIT; response envelope with section_number, section_title, status, version, word_count) in `src/Ato.Copilot.Agents/Compliance/Tools/SspAuthoringTools.cs`
- [x] T011 [P] [US1] Create `ReviewSspSectionTool` class extending BaseTool (Name="compliance_review_ssp_section", RequiredPimTier=PimTier.Write, Parameters: system_id, section_number, decision ["approve"|"request_revision"], reviewer, comments [required if rejecting]; error codes: SECTION_NOT_FOUND, INVALID_STATUS_FOR_REVIEW, COMMENTS_REQUIRED, INVALID_DECISION) in `src/Ato.Copilot.Agents/Compliance/Tools/SspAuthoringTools.cs`
- [x] T012 [P] [US1] Create `SspCompletenessTool` class extending BaseTool (Name="compliance_ssp_completeness", RequiredPimTier=PimTier.Read, Parameter: system_id; returns system_name, overall_readiness_percent, approved_count, total_sections=13, sections array, blocking_issues array) in `src/Ato.Copilot.Agents/Compliance/Tools/SspAuthoringTools.cs`
- [x] T013 [US1] Register `WriteSspSectionTool`, `ReviewSspSectionTool`, `SspCompletenessTool` as singletons with BaseTool forwarding pattern in `src/Ato.Copilot.Agents/Extensions/ServiceCollectionExtensions.cs`

### Tests

- [x] T014 [P] [US1] Unit tests for section management service methods: WriteSspSectionAsync (create new, update existing, concurrency conflict rejection, auto-gen section stores generated content, authored section requires content, HasManualOverride on auto-gen with user content, Version increment, Status reset to Draft on update, submit_for_review transitions Draft→UnderReview, submit_for_review rejects non-Draft status with INVALID_STATUS_FOR_SUBMIT); ReviewSspSectionAsync (approve from UnderReview, reject with comments, reject without comments fails, reject from Draft fails, reject from NotStarted fails); GetSspCompletenessAsync (empty system returns 13 NotStarted, partial system calculates correct readiness %, blocking issues list) in `tests/Ato.Copilot.Tests.Unit/Services/SspServiceSectionTests.cs`
- [x] T015 [P] [US1] Unit tests for section management tools: WriteSspSectionTool (valid write, invalid section number, missing content for authored, submit_for_review success, submit_for_review invalid status), ReviewSspSectionTool (approve, reject, invalid decision), SspCompletenessTool (returns correct structure) using mocked ISspService in `tests/Ato.Copilot.Tests.Unit/Tools/SspAuthoringToolTests.cs`

**Checkpoint**: SSP section CRUD + lifecycle + completeness tracking fully functional and tested. MVP deliverable.

---

## Phase 4: User Story 2 — Complete SSP Section Generation (Priority: P2)

**Goal**: Implement all missing NIST 800-18 section generators so SspService can produce content for all 13 sections.

**Independent Test**: Call WriteSspSectionAsync for each auto-generated section number → verify output contains expected data from test entities. Call for authored sections with content → verify stored correctly.

### Implementation

- [x] T016 [US2] Enhance §1 System Identification generator to include DitprId and EmassId from RegisteredSystem and enhance §2 Security Categorization generator to include FIPS 200 minimum security requirements reference in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T017 [US2] Implement §3 System Owner / AO auto-generator (structured section with System Owner, AO, ISSO, ISSM, other key personnel from RmfRoleAssignment with contact information; distinct from §1 key personnel table — includes detailed role descriptions and responsibilities per NIST 800-18 §2.3) and §4 Information System Type auto-generator (system type Major Application vs General Support System, mission criticality from RegisteredSystem, OperationalStatus, OperationalDate, DisposalDate per Cap 2.0c) in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T018 [US2] Implement §5 General Description / Purpose authored section generator (return SspSection.Content if authored; use RegisteredSystem.Description as starting template if exists; "[Section not yet authored]" placeholder otherwise) and §8 Related Laws / Regulations authored section generator (return SspSection.Content; placeholder if not authored) in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T019 [US2] Implement §6 System Environment hybrid generator: auto-populate cloud region from RegisteredSystem.HostingEnvironment; merge with SspSection authored narrative for physical security, logical environment, hardware/software inventory summary, network topology; output structured metadata + narrative in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T020 [US2] Implement §7 System Interconnections auto-generator: reposition Feature 021 interconnections content to §7 per NIST 800-18; render SystemInterconnection + InterconnectionAgreement data as table (connection type, target system, data flow direction, classification, agreement status, security measures) plus narrative summary of information sharing policies; support HasManualOverride for editable override in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T021 [US2] Implement §9 Minimum Security Controls enhanced auto-generator: combine ControlBaseline (selected baseline level, overlay applied) + ControlTailoring (added/removed controls with rationale per Research R10) + ControlImplementation (total count by responsibility type); output baseline level, overlay, control counts, tailored controls summary, compensating controls, link to §10 full detail in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T022 [US2] Implement §11 Authorization Boundary auto-generator: resource inventory table from AuthorizationBoundary entities (Azure resource ID, resource type, display name, in-boundary status, inheritance provider); authored boundary narrative from SspSection; exclusion rationale for out-of-boundary resources; total resource count by type in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T023 [US2] Implement §12 Personnel Security authored generator (auto-populated role list from RmfRoleAssignment + authored screening requirements, access agreements, training requirements, separation procedures from SspSection) and §13 Contingency Plan authored generator (ContingencyPlanReference metadata: document title, location, version, last test date, test type, RTO, RPO, alternate processing site, backup summary + authored narrative from SspSection) in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`

### Tests

- [x] T024 [P] [US2] Unit tests for all section generators: §1 with DitprId/EmassId, §2 with FIPS 200 ref, §3 from RmfRoleAssignment, §4 system type with OperationalStatus/OperationalDate/DisposalDate, §5 authored with template fallback, §6 hybrid merge, §7 interconnections repositioned, §8 authored, §9 with ControlTailoring rationale, §11 boundary resource inventory, §12 personnel with roles, §13 contingency plan metadata; test empty data scenarios return placeholders in `tests/Ato.Copilot.Tests.Unit/Services/SspServiceSectionTests.cs`

**Checkpoint**: All 13 section generators implemented. SspService can produce content for every NIST 800-18 section.

---

## Phase 5: User Story 3 — OSCAL 1.1.2 SSP Export (Priority: P3)

**Goal**: Produce schema-compliant OSCAL 1.1.2 SSP JSON with all 6 required top-level sections from entity data.

**Independent Test**: Export OSCAL for a system with complete data → verify JSON contains system-security-plan with metadata (oscal-version=1.1.2, roles, parties), import-profile (correct baseline URI), system-characteristics, system-implementation (components, users), control-implementation, back-matter. Verify statistics counts match entity counts.

### Implementation

- [x] T025 [P] [US3] Create `IOscalSspExportService` interface with `ExportAsync(string registeredSystemId, bool includeBackMatter = true, bool prettyPrint = true, CancellationToken cancellationToken = default)` method returning `Task<OscalExportResult>`; add `OscalExportResult` record (OscalJson string, Warnings List\<string\>, Statistics OscalStatistics); add `OscalStatistics` record (ControlCount, ComponentCount, InventoryItemCount, UserCount, BackMatterResourceCount) in `src/Ato.Copilot.Agents/Compliance/Services/IOscalSspExportService.cs`
- [x] T026 [US3] Create `OscalSspExportService` class implementing `IOscalSspExportService`: constructor DI (IServiceScopeFactory, ILogger\<OscalSspExportService\>); static `JsonSerializerOptions` (PropertyNamingPolicy=JsonNamingPolicy.KebabCaseLower, WriteIndented=true, DefaultIgnoreCondition=WhenWritingNull); static baseline profile URI constants per Research R2 (Low, Moderate, High) in `src/Ato.Copilot.Agents/Compliance/Services/OscalSspExportService.cs`
- [x] T027 [US3] Implement OSCAL `metadata` builder method: generate title ("{SystemName} System Security Plan"), last-modified (ISO 8601 UTC), version (from system or "1.0"), oscal-version "1.1.2", roles array mapped from RmfRoleAssignment (ISSO/ISSM/AO/SO with UUIDs), parties array (persons with name + org), responsible-parties linking role-ids to party UUIDs; empty arrays + warning if no RmfRoleAssignment in `src/Ato.Copilot.Agents/Compliance/Services/OscalSspExportService.cs`
- [x] T028 [US3] Implement OSCAL `import-profile` builder: href from static baseline URI constant based on ControlBaseline.BaselineLevel (Low/Moderate/High); include overlay reference if ControlBaseline.OverlayApplied is set; placeholder handling per Research R4: omit href + add warning if no ControlBaseline found in `src/Ato.Copilot.Agents/Compliance/Services/OscalSspExportService.cs`
- [x] T029 [US3] Implement OSCAL `system-characteristics` builder: system-name, system-name-short (acronym), description from RegisteredSystem; security-sensitivity-level from SecurityCategorization (placeholder "not-yet-determined" if missing); system-information with information-types (C/I/A from InformationType); security-impact-level from FIPS 199 levels (default "low" if missing); authorization-boundary description from §11 content; network-architecture description from §6 content; data-flow description from interconnections in `src/Ato.Copilot.Agents/Compliance/Services/OscalSspExportService.cs`
- [x] T030 [US3] Implement OSCAL `system-implementation` builder: components from AuthorizationBoundary entities (each resource → component with UUID, type, title, description, status "operational"); inventory-items from boundary resources with component references; users from RmfRoleAssignment (role-id, privilege-level); leveraged-authorizations from ControlInheritance providers (CSP FedRAMP auth refs); empty arrays if no data per Research R4 in `src/Ato.Copilot.Agents/Compliance/Services/OscalSspExportService.cs`
- [x] T031 [US3] Implement OSCAL `control-implementation` builder: description field; implemented-requirements from ControlImplementation records (uuid, control-id, description/narrative, props with implementation-status, responsible-roles from inheritance type, by-components linking to system-implementation component UUIDs, statements for multi-part controls); empty array + warning if no narratives; and implement OSCAL `back-matter` builder: resources array with ISA/MOU from InterconnectionAgreement document refs, PIA document ref, contingency plan from ContingencyPlanReference, SSP self-reference; each resource with UUID, title, description, rlinks (href, media-type) in `src/Ato.Copilot.Agents/Compliance/Services/OscalSspExportService.cs`
- [x] T032 [US3] Implement `ExportAsync` method: create scope via IServiceScopeFactory, load all entity data (RegisteredSystem + includes), call each OSCAL section builder, assemble under `system-security-plan` root with UUID, apply incomplete data placeholder strategy per Research R4 (schema-valid defaults for every missing piece), collect all warnings, compute OscalStatistics from generated sections, serialize to JSON with configured options, respect includeBackMatter and prettyPrint parameters in `src/Ato.Copilot.Agents/Compliance/Services/OscalSspExportService.cs`
- [x] T033 [P] [US3] Create `ExportOscalSspTool` class extending BaseTool (Name="compliance_export_oscal_ssp", RequiredPimTier=PimTier.Read, Parameters: system_id [required], include_back_matter [bool, default true], pretty_print [bool, default true]; calls OscalSspExportService.ExportAsync; returns oscal_ssp_json, warnings, statistics per contracts Tool 4) in `src/Ato.Copilot.Agents/Compliance/Tools/SspAuthoringTools.cs`
- [x] T034 [P] [US3] Modify `EmassExportService.BuildOscalSsp` to delegate SSP OSCAL generation to `IOscalSspExportService.ExportAsync` (inject IOscalSspExportService via constructor; when model type is SSP, call ExportAsync and return its JSON; Assessment Results and POA&M generation remain unchanged) in `src/Ato.Copilot.Agents/Compliance/Services/EmassExportService.cs`
- [x] T035 [US3] Register `OscalSspExportService` as singleton implementing `IOscalSspExportService` and register `ExportOscalSspTool` with BaseTool forwarding pattern in `src/Ato.Copilot.Agents/Extensions/ServiceCollectionExtensions.cs`

### Tests

- [x] T036 [P] [US3] Unit tests for OSCAL SSP export: metadata with roles/parties from RmfRoleAssignment; import-profile with correct baseline URI per level; system-characteristics with categorization and boundary; system-implementation with components/users/inventory; control-implementation with by-components cross-refs; back-matter with document references; ExportAsync with complete data (6 sections present); ExportAsync with missing data (placeholders + warnings per Research R4); oscal-version = "1.1.2"; JSON uses kebab-case naming in `tests/Ato.Copilot.Tests.Unit/Services/OscalSspExportServiceTests.cs`

**Checkpoint**: OSCAL 1.1.2 SSP JSON export fully functional with placeholder strategy for incomplete data.

---

## Phase 6: User Story 4 — OSCAL Structural Validation (Priority: P4)

**Goal**: Validate OSCAL SSP JSON for structural correctness before FedRAMP/eMASS submission.

**Independent Test**: Generate OSCAL from complete system → validate → IsValid=true with statistics. Remove system-implementation → validate → error "Required section 'system-implementation' is missing". Add invalid component UUID ref → validate → warning.

### Implementation

- [x] T037 [P] [US4] Create `IOscalValidationService` interface with `ValidateSspAsync(string oscalJson, CancellationToken cancellationToken = default)` returning `Task<OscalValidationResult>`; add `OscalValidationResult` record (IsValid bool, Errors List\<string\>, Warnings List\<string\>, Statistics OscalStatistics) in `src/Ato.Copilot.Agents/Compliance/Services/IOscalValidationService.cs`
- [x] T038 [US4] Implement `OscalValidationService`: deserialize JSON, run 7 structural checks: (1) top-level `system-security-plan` key present, (2) required child sections present (metadata, import-profile, system-characteristics, system-implementation, control-implementation), (3) all UUID values are valid GUID format, (4) control-id values in implemented-requirements match import-profile baseline controls, (5) component UUIDs in by-components exist in system-implementation.components, (6) party UUIDs in responsible-parties exist in metadata.parties, (7) oscal-version equals "1.1.2"; populate Errors (structural failures), Warnings (non-blocking), Statistics (counts) in `src/Ato.Copilot.Agents/Compliance/Services/OscalValidationService.cs`
- [x] T039 [US4] Create `ValidateOscalSspTool` class extending BaseTool (Name="compliance_validate_oscal_ssp", RequiredPimTier=PimTier.Read, Parameter: system_id; generates OSCAL via IOscalSspExportService.ExportAsync then validates via IOscalValidationService.ValidateSspAsync; returns is_valid, errors, warnings, statistics per contracts Tool 5) in `src/Ato.Copilot.Agents/Compliance/Tools/SspAuthoringTools.cs`
- [x] T040 [US4] Register `OscalValidationService` as singleton implementing `IOscalValidationService` and register `ValidateOscalSspTool` with BaseTool forwarding in `src/Ato.Copilot.Agents/Extensions/ServiceCollectionExtensions.cs`

### Tests

- [x] T041 [P] [US4] Unit tests for OSCAL validation: valid SSP passes all checks (IsValid=true, empty errors); missing system-security-plan key (error); missing required child section (error per section); invalid UUID format (error); control-id mismatch (warning); component UUID not found (warning); party UUID not found (warning); wrong oscal-version (warning); statistics populated correctly in `tests/Ato.Copilot.Tests.Unit/Services/OscalValidationServiceTests.cs`

**Checkpoint**: OSCAL validation catches structural errors before submission. Export + validate pipeline complete.

---

## Phase 7: User Story 5 — Enhanced SSP Generation (Priority: P5)

**Goal**: Upgrade GenerateSspAsync to produce a complete 13-section SSP document with correct NIST 800-18 numbering, metadata, and completeness warnings.

**Independent Test**: Call GenerateSspAsync with no sections filter → output contains §1–§13 in order with correct titles. Call with old key "baseline" → maps to §9 minimum_controls. Verify YAML front-matter block with metadata. Verify warnings for missing/unapproved sections.

### Implementation

- [x] T042 [US5] Implement section key renumbering with backward-compatible key map: system_information→system_identification (§1), categorization unchanged (§2), new keys personnel (§3), system_type (§4), description (§5), environment (§6), interconnections unchanged (§7), laws_regulations (§8), baseline→minimum_controls (§9), controls→control_implementations (§10), authorization_boundary (§11), personnel_security (§12), contingency_plan (§13); add lookup method that accepts old or new key per Research R9 in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T043 [US5] Rewrite `GenerateSspAsync` to assemble all 13 sections in NIST 800-18 order: call auto-generators for §1,§2,§3,§4,§7,§9,§10,§11; pull SspSection.Content for authored §5,§8,§12,§13 (and hybrid §6); output "[Section not yet authored]" placeholder with warning for missing authored content; support selective generation via sections parameter using new + old keys with backward compatibility map in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T044 [US5] Add YAML front-matter metadata block to generated SSP output: document_version, generated_at (ISO 8601), system_name, system_id, categorization_level, baseline_level, narrative_completion_percent, section_completion (approved_count/13) in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T045 [US5] Add completeness warnings to generated SSP: missing/not-started sections highlighted with "[NOT STARTED]" markers, unapproved sections flagged with "[UNAPPROVED]", auto-generated sections with HasManualOverride noted with "[MANUAL OVERRIDE]", narrative completion percentage per control family in §10 in `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs`
- [x] T046 [US5] Update `GenerateSspTool` (existing compliance_generate_ssp) sections parameter to accept 13 new section keys plus 3 backward-compatible old keys per Cap 5.3; update parameter description and validation in `src/Ato.Copilot.Agents/Compliance/Tools/SspAuthoringTools.cs`

**Checkpoint**: Full 13-section SSP generation with NIST 800-18 numbering, metadata, and completeness warnings.

---

## Phase 8: Polish & Cross-Cutting Concerns

**Purpose**: Integration tests, build verification, and agent context update

- [x] T047 [P] Integration tests for SSP section management + OSCAL export + validation end-to-end: create system with seed data → write authored sections → review/approve → check completeness (readiness %) → generate 13-section SSP → export OSCAL JSON → validate structure; test RBAC (PimTier.Read cannot write, PimTier.Read can export+validate); test backward compatibility with old section keys in `tests/Ato.Copilot.Tests.Integration/Tools/SspToolsIntegrationTests.cs`
- [x] T048 Verify `dotnet build Ato.Copilot.sln` succeeds with zero errors and zero warnings
- [x] T049 End-to-end smoke test: write SSP §6 (authored) → update §6 → review §6 (approve) → write §11 (auto-gen) → check completeness → generate full 13-section SSP → export OSCAL SSP JSON → validate OSCAL → verify AT-01 through AT-30 scenarios covered
- [x] T050 Verify unit test coverage ≥80% on new service files by running `dotnet test --collect:"XPlat Code Coverage"` and checking coverage report for SspService, OscalSspExportService, and OscalValidationService per SC-009
- [x] T051 Update agent context by running `pwsh -File .specify/scripts/powershell/update-agent-context.ps1 -AgentType copilot`

---

## Dependencies

### Story Completion Order

```
Phase 2 (Foundational)
    │
    ├──► Phase 3: US1 (Section Management) ──► Phase 7: US5 (Enhanced Generation)
    │                                                │
    ├──► Phase 4: US2 (Section Generators) ──────────┤
    │                                                │
    ├──► Phase 5: US3 (OSCAL Export) ──► Phase 6: US4 (OSCAL Validation)
    │
    └──► Phase 8 (Polish) — after all stories complete
```

### Key Dependencies

| Task | Depends On | Reason |
|------|-----------|--------|
| T007–T009 | T002–T006 | Service methods need entities + interface signatures |
| T010–T012 | T006 | Tools call ISspService methods (can parallel with T007–T009) |
| T016–T023 | T003–T005 | Section generators need SspSection entity + DbContext |
| T025 | T002–T006 | Interface file needs OscalStatistics record types |
| T026–T032 | T025 | Service implements interface |
| T033–T034 | T032 | Tool and delegation need ExportAsync |
| T037 | T025 | Shares OscalStatistics record |
| T038 | T037 | Service implements interface |
| T039 | T038, T032 | Tool needs both export and validation services |
| T042–T046 | T007–T009, T016–T023 | Enhanced generation needs section management + all generators |

### Parallel Execution Opportunities

**Within Phase 3 (US1)**: T010+T011+T012 (tools) can run in parallel with T007+T008+T009 (service methods) since tools depend on ISspService interface (T006), not implementations.

**Within Phase 5 (US3)**: T033 (export tool) and T034 (EmassExportService delegation) can run in parallel after T032 (ExportAsync) is complete — different files, both depend on the export service.

**Across Phases**: US1 (Phase 3) and US3 (Phase 5) can start simultaneously after Phase 2 — they operate on different files with no shared implementation dependencies.

**Test tasks**: All test tasks (T014, T015, T024, T036, T041, T047) are [P] — they can run in parallel with implementation once their story's implementation is complete.

---

## Implementation Strategy

### MVP Scope (Phase 1–3)

User Story 1 alone delivers the core workflow: ISSO authors sections, ISSM reviews, everyone checks completeness. This is the minimum shippable increment.

### Incremental Delivery

1. **Phase 1–3**: Section management MVP — author, review, track
2. **Phase 4**: Section generators — auto-populate 8 sections from existing data
3. **Phase 5–6**: OSCAL export + validation — machine-readable output for FedRAMP/eMASS
4. **Phase 7**: Enhanced generation — full 13-section assembly with metadata
5. **Phase 8**: Polish, integration tests, final verification

### File Inventory

| File | Status | Phases |
|------|--------|--------|
| `src/Ato.Copilot.Core/Models/Compliance/RmfModels.cs` | Modified | 2 |
| `src/Ato.Copilot.Core/Data/Context/AtoCopilotContext.cs` | Modified | 2 |
| `src/Ato.Copilot.Agents/Compliance/Services/ISspService.cs` | Modified | 2 |
| `src/Ato.Copilot.Agents/Compliance/Services/SspService.cs` | Modified | 3, 4, 7 |
| `src/Ato.Copilot.Agents/Compliance/Services/IOscalSspExportService.cs` | **New** | 5 |
| `src/Ato.Copilot.Agents/Compliance/Services/OscalSspExportService.cs` | **New** | 5 |
| `src/Ato.Copilot.Agents/Compliance/Services/IOscalValidationService.cs` | **New** | 6 |
| `src/Ato.Copilot.Agents/Compliance/Services/OscalValidationService.cs` | **New** | 6 |
| `src/Ato.Copilot.Agents/Compliance/Services/EmassExportService.cs` | Modified | 5 |
| `src/Ato.Copilot.Agents/Compliance/Tools/SspAuthoringTools.cs` | Modified | 3, 5, 6, 7 |
| `src/Ato.Copilot.Agents/Extensions/ServiceCollectionExtensions.cs` | Modified | 3, 5, 6 |
| `tests/Ato.Copilot.Tests.Unit/Services/SspServiceSectionTests.cs` | **New** | 3, 4 |
| `tests/Ato.Copilot.Tests.Unit/Services/OscalSspExportServiceTests.cs` | **New** | 5 |
| `tests/Ato.Copilot.Tests.Unit/Services/OscalValidationServiceTests.cs` | **New** | 6 |
| `tests/Ato.Copilot.Tests.Unit/Tools/SspAuthoringToolTests.cs` | Modified | 3 |
| `tests/Ato.Copilot.Tests.Integration/Tools/SspToolsIntegrationTests.cs` | **New** | 8 |
