# Feature 051 / US1 — First-Time Login from the Dashboard (P1)

> **Parent**: #68
> **Phase**: 3 (T041–T054)
> **Story FRs**: FR-001, FR-002, FR-003, FR-004
> **Quickstart**: [specs/051-login/quickstart.md §§ 1, 2, 4](../specs/051-login/quickstart.md)

## Status

**Code-complete + automated tests green; manual sign-off (T054) pending.**

## Story

A user opens the Dashboard URL. Because there is no active session,
they are routed to a branded `/login` page that explains the deployment
("Coastal Watch — ATO Copilot") and shows the **deployment's primary
auth method as configured by `Auth:DefaultMethod`** (CAC or Entra).
Any other configured methods appear as secondary buttons. After
authenticating, they land on the page they originally requested
(deep-link preservation), not the dashboard root.

## Acceptance scenarios (from spec)

1. **Given** an unauthenticated user navigating to any protected route, **When** the React router resolves the route, **Then** the user is redirected to `/login?return=<encoded-original-url>`.
2. **Given** the `/login` page is open, **When** it renders, **Then** the primary button is the method named in `Auth:DefaultMethod`, and any other enabled methods (CAC, Entra, Simulation when allowed) appear as secondary actions.
3. **Given** the user clicks the primary auth button, **When** MSAL completes successfully, **Then** the SPA stores the session, calls `/api/auth/me`, and navigates to the original URL stored in `return`.
4. **Given** sign-in succeeds but the original URL is missing or invalid, **When** routing resolves, **Then** the user lands on the persona-default landing page from 015-persona-workflows.
5. **Given** the deployment has no branding configured, **When** the page renders, **Then** it falls back to "ATO Copilot" without a missing-asset broken layout.

## Functional requirements covered

- **FR-001**: Redirect unauthenticated requests to `/login?return=<URL-encoded-path>`.
- **FR-002**: Branding (name, logo) sourced from configuration; missing branding falls back without broken-image artifacts.
- **FR-003**: Primary CTA is the configured `Auth:DefaultMethod`; disabled methods MUST NOT appear.
- **FR-004**: Post-sign-in navigate to `return`; on missing/malformed/external, use persona-default landing.

## Tasks (Phase 3 + Phase 3.5 + Phase 3.6)

- [X] T041 [TDD-Test] `LoginConfigEndpointTests`
- [X] T042 Endpoint `GET /api/auth/login-config`
- [X] T043 [TDD-Test] `MeEndpointTests`
- [X] T044 Endpoint `GET /api/auth/me`
- [X] T045 Wire both endpoints in Program.cs
- [X] T046–T049 LoginConfigContext, MsalProvider scope, deep-link preserving login redirect
- [X] T050–T053 LoginPage, LoginCallbackPage, RequireAuth router guard
- [X] T053a–T053c cross-tab login race listener
- [ ] T054 Execute quickstart §§ 1, 2, 4 against live docker stack

## Suggested labels

- `area:auth`, `story:US1`, `priority:p1`
