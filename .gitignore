# Build artifacts
bin/
obj/
publish/
*.lscache

# IDE
.vs/
*.user
*.suo

# VS Code: track shared workspace settings, ignore user-local overrides
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json

# Environment
.env
*.local

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log

# Database
*.db
*.db-shm
*.db-wal

# Secrets
appsettings.*.local.json
*.pfx
*.pem

# Docker
docker-compose.override.yml

# Export data (generated SSP files and templates)
# NOTE: Leading `/` anchors this to the repo root only. Without it, the rule
# also matches every `/Data/` subdirectory on case-insensitive filesystems
# (macOS APFS, Windows NTFS) and silently drops new files there from `git add`.
/data/

# NuGet
*.nupkg
nuget.config

# Node.js / React
node_modules/
dist/
build/
*.tsbuildinfo

# Test results
TestResults/
coverage/

# MkDocs output
site/
