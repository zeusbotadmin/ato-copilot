# Feature 051 / US6 — Login from M365 / Teams Bot (P2)

> **Parent**: #<feature-051-parent-issue-id>
> **Phase**: 9 (T112–T119)
> **Story FRs**: FR-020, FR-021, FR-022
> **Quickstart**: [specs/051-login/quickstart.md § 9](../specs/051-login/quickstart.md)

## Status

**Code-complete + automated tests green; manual sign-off (T119) pending.**

## Story

A Teams user `@mentions` the ATO Copilot bot for the first time. The
bot returns an Adaptive Card with a "Sign in" button that triggers a
Bot Framework SSO token exchange (or, for tenants without SSO, an
OAuthPrompt). Once signed in, the user's Teams identity is linked to
their ATO Copilot identity and subsequent messages are silently
authenticated.

## Acceptance scenarios

1. **Given** the bot is installed in a Teams tenant, **When** a user `@mentions` it without an existing link, **Then** the bot replies with an Adaptive Card containing a sign-in button — never a free-text prompt.
2. **Given** Teams SSO is enabled in the bot manifest, **When** the user clicks the sign-in button, **Then** the token exchange completes silently using their existing Teams session.
3. **Given** the link is established, **When** the user messages from a different Teams client (mobile vs desktop), **Then** the same identity link applies — no re-prompt.
4. **Given** the user is signed in but the impersonated tenant has been disabled, **When** they message the bot, **Then** the bot replies with the standard "Tenant disabled" error from Feature 048.

## Functional requirements covered

- **FR-020**: First-mention by an unlinked Teams user MUST trigger an Adaptive Card with a sign-in button — never a free-text prompt.
- **FR-021**: `Auth:TeamsSso:Mode` defaults to `Optional` (deployment-wide; no per-tenant override). When `Required`, deployments without an SSO-enabled manifest MUST fail at startup. When `Disabled`, the OAuthPrompt path MUST always run.
- **FR-022**: Identity links MUST be Teams-tenant-wide, not per-client. One sign-in MUST satisfy mobile, desktop, and web.

## Tasks (Phase 9)

- [X] T112 [TDD-Test] `TeamsManifestValidatorTests` — Required mode startup-fail without SSO entry
- [X] T113 Manifest validator implementation
- [X] T114 Auth dispatcher (SSO → OAuthPrompt fallback per `TeamsSso:Mode`)
- [X] T115 `IIdentityLinkStore` for Teams-tenant-wide identity linking
- [X] T116–T117 OAuthPrompt fallback wiring + Adaptive Card sign-in template
- [X] T118 "Tenant disabled" error path via Feature 048
- [ ] T119 Execute quickstart § 9 — **requires deployed Teams app + bot tunnel**

## Suggested labels

- `area:auth`, `area:m365-teams`, `story:US6`, `priority:p2`
