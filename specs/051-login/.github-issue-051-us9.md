# Feature 051 / US9 — Account Menu and Profile Surface (P3)

> **Parent**: #68
> **Phase**: 12 (T137–T141)
> **Story FRs**: FR-030, FR-031
> **Quickstart**: [specs/051-login/quickstart.md § 12](../specs/051-login/quickstart.md)

## Status

**Code-complete + automated tests green; manual sign-off (T141) pending.**

## Story

The top-right account menu shows the signed-in user's display name,
persona (015-persona-workflows), home tenant, active PIM roles
(003-cac-auth-pim), and links to "My profile", "Switch tenant",
"Sign out". Hovering a PIM role shows its expiration time. The menu is
keyboard-navigable and screen-reader friendly.

## Acceptance scenarios

1. **Given** an authenticated user, **When** they click their avatar, **Then** the menu shows display name, persona, home tenant, and any active PIM role with expiration.
2. **Given** a CSP-Admin in impersonation, **When** they open the menu, **Then** the menu shows both their real identity AND the impersonated tenant.
3. **Given** keyboard navigation, **When** the user tabs into the menu, **Then** all menu items are reachable via Tab / Shift-Tab and dismissible via Esc.
4. **Given** a screen reader, **When** the menu opens, **Then** ARIA attributes correctly announce role, expiration, and tenant.

## Functional requirements covered

- **FR-030**: Top-right account menu shows display name, persona, home tenant, and any active PIM role with expiration.
- **FR-031**: Menu MUST be keyboard-navigable (Tab / Shift-Tab / Esc) and ARIA-compliant.

## Tasks (Phase 12)

- [X] T137 [TDD-Test] `AccountMenu.test.tsx`
- [X] T138 `useMe` hook
- [X] T139 Full `AccountMenu` with persona, tenant, PIM-role list, ARIA roles
- [X] T140 1-minute live countdown ticker for PIM expirations
- [ ] T141 Execute quickstart § 12 — **requires live deployment with a real PIM role assignment**

## Note

In Phase 13.2 (T144a) the menu's PIM-role DOM was refactored from
`<ul>` / `<li role="menuitem">` to `<div role="group">` /
`<div role="menuitem">` to satisfy axe-core's
`aria-required-parent` + `list-only-li` + `aria-required-children`
rules. Keyboard navigation invariants are preserved (the existing
`AccountMenu.test.tsx` suite still passes).

## Suggested labels

- `area:auth`, `area:ui`, `story:US9`, `priority:p3`
