# Feature 051 / US10 — Audit Trail of Every Login Event (P1)

> **Parent**: #<feature-051-parent-issue-id>
> **Phase**: 7 (T085–T100) — plus Phase 13.1 throttle middleware (T142–T144)
> **Story FRs**: FR-032, FR-033, FR-034, FR-035, FR-036, FR-036a, FR-038
> **Quickstart**: [specs/051-login/quickstart.md §§ 15, 16](../specs/051-login/quickstart.md)

## Status

**Code-complete + automated tests green; manual sign-off (T100) pending.**

## Story

Every authentication event (success, failure, sign-out, **idle
sign-out**, impersonation start, impersonation end, tenant switch,
simulated login, blocked simulation attempt) writes a structured audit
row containing user `oid`, `tid`, `correlationId`, source IP, user
agent, surface (Dashboard / VS Code / M365 / Chat), and event type.
Failed logins are rate-limited per identity + per IP using
**environment-specific thresholds** drawn from
`Auth:Throttle:Development` or `Auth:Throttle:Production`. Rows are
retained for 13 months in the hot operational table; older rows
migrate to an immutable append-only cold archive (Azure Storage
append-blob in production, local filesystem under
`archive/LoginAuditEvents/{yyyy}/{MM}/` in dev).

## Acceptance scenarios

1. **Given** a successful login, **When** the audit subscriber fires, **Then** a row exists with `eventType=LoginSuccess`, full identity, surface, and correlation ID.
2. **Given** a failed login, **When** the audit row is written, **Then** the row contains the **error class** (e.g., `CertExpired`) but **NOT** the cert thumbprint or any PII beyond `oid`.
3. **Given** failed-login throttling configured separately for dev and prod, **When** the threshold for the active environment is exceeded, **Then** the server returns `429` with `Retry-After` + the user-facing message "Too many sign-in attempts. Try again in {N} minutes."
4. **Given** a CSP-Admin impersonation event, **When** the audit row is written, **Then** it includes both the real `oid` and `effective.tenantId` per Feature 048's audit contract.
5. **Given** an idle sign-out fires (per US2), **When** the audit row is written, **Then** `eventType=IdleSignOut` is recorded with the session start time + idle duration.
6. **Given** a `POST /api/auth/simulate` arrives outside `Development`, **When** the request is rejected with 404, **Then** an audit row `eventType=SimulationBlocked` is written with `severity=Security`.

## Functional requirements covered

- **FR-032**: Every authentication event writes one structured row to `LoginAuditEvents` with full identity, correlation, surface, and event-type. `EffectiveTenantId` is non-null on every row (`SYSTEM_TENANT_ID` for pre-session / unmapped events).
- **FR-033**: Failed-login audit rows MUST contain the error class but MUST NOT contain cert thumbprints or any PII beyond `oid`.
- **FR-034**: Failed logins throttled per identity AND per source IP. Defaults: Development 100/min/IP + 100/min/identity; **every non-Development env uses Production** (analysis C11): 20/min/IP + 10/min/identity.
- **FR-035**: When throttled, server returns `429 TOO_MANY_LOGINS` + `Retry-After` header + the user-facing message.
- **FR-036**: Throttle counter backed by a persistent / distributed store (Redis in production, in-memory in dev/test) so a process restart cannot bypass throttling.
- **FR-036a**: 13-month hot retention + immutable cold archive (Azure Storage append-blob in production, local filesystem under `archive/LoginAuditEvents/{yyyy}/{MM}/` in dev). Daily background job archives rows older than 13 months in 1,000-row batches.
- **FR-038**: All login endpoints emit Serilog structured logs with `correlationId`, `surface`, `eventType`; redact bearer / refresh tokens, cookie values, cert thumbprints. Per the Phase 13.3 audit, the only known leak (an oid in a `LogDebug` cache-key message) was fixed in commit 61096dd.

## Tasks (Phase 7 + Phase 13.1)

- [X] T085 [TDD-Test] `LoginAuditServiceReadTests`
- [X] T086 Audit read service (`ListAsync` + `ListSystemTenantAsync`)
- [X] T087 `Auth.SocAnalyst` claim mapping at the Entra group→role layer
- [X] T088 [TDD-Test] `LoginAuditEventsEndpointTests`
- [X] T089–T090 `GET /api/auth/events` read endpoint (CSP-Admin / SOC-analyst gated)
- [X] T091–T095 Archive sinks — `FileSystemArchiveSink` + `AzureBlobAppendArchiveSink`
- [X] T096–T099 `LoginAuditArchiveService` hosted service + DI wiring
- [X] T142 [TDD-Test] [Phase 13.1] `LoginThrottleMiddlewareTests`
- [X] T143 [Phase 13.1] `LoginThrottleMiddleware` — peek-and-register pattern per analysis C17
- [X] T144 [Phase 13.1] Wire middleware after `CacAuthenticationMiddleware`
- [ ] T100 Execute quickstart §§ 15, 16 — **pending live verification.** Code paths + automated coverage are landed; end-to-end SOC-analyst playthrough + 13-month retention sweep validation require a live Foundry deployment

## Suggested labels

- `area:auth`, `area:audit`, `area:security`, `story:US10`, `priority:p1`
